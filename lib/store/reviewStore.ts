/**
 * Review Store - Re-export from domains/reviews
 * Legacy compatibility for existing imports
 */

// Re-export the review store from the domains folder
export { useReviewStore } from './domains/reviews/reviewStore';
